# Use the official Apache Superset image as the base
FROM apache/superset:latest

# Set environment variables (optional)
ENV SUPERSET_ENV=prod

# Copy custom configuration files (if any)
# COPY superset_config.py /app/

# Run any additional setup commands (optional)
RUN pip install --no-cache-dir some-additional-package
